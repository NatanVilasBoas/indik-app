import { styled } from "styled-components/native";

export const IconText = styled.Text`
  font-size: 16px;
  font-family: ${(props) => props.theme.fonts.PlusJakartaSansRegular};
  font-size: ${(props) => props.theme.fonts.sizeLarge}px;
`;
